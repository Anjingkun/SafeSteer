# Trained model artifacts (huge, never tracked)
model_weights/
*.pt
*.bin
*.safetensors

# Training / experiment outputs
wandb/
logs/
outputs/

# Python caches
__pycache__/
*.py[cod]
*.so
.pytest_cache/

# Editor / OS
.vscode/
.idea/
.DS_Store
*.swp

# Local environment / secrets
.env
.envrc

# Local-only backup file (kept on disk, never tracked)
distil_trainer_copy.py
